name: CI

on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      release:
        description: 'For create a release? Set to "true" to publish.'
        required: false
        default: 'false'
      version:
        description: 'Release version tag, e.g. v1.2.3 (required when release=true for manual run)'
        required: false
        default: ''

jobs:
  security_scan:
    name: Security scan (gitleaks)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Run gitleaks scan
        uses: zricethezav/gitleaks-action@v2
      - name: Upload gitleaks report
        uses: actions/upload-artifact@v4
        with:
          name: gitleaks-report
          path: results.sarif

  build_apk:
    name: Build APK
    runs-on: ubuntu-latest
    needs: security_scan
    steps:
      - uses: actions/checkout@v4
      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.0'
          channel: 'stable'
          architecture: x64
      - name: Flutter pub get
        run: flutter pub get
      - name: Build release APK
        run: flutter build apk --release -v
      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: build/app/outputs/flutter-apk/app-release.apk

  publish_release:
    name: Publish release (conditional)
    runs-on: ubuntu-latest
    needs: build_apk
    if: >-
      (github.event_name == 'workflow_dispatch' && github.event.inputs.release == 'true') ||
      (github.event_name == 'push' && contains(github.event.head_commit.message, 'release'))
    steps:
      - uses: actions/checkout@v4
      - name: Download APK artifact
        uses: actions/download-artifact@v4
        with:
          name: android-apk
          path: release_artifacts
      - name: Determine release tag
        id: determine_tag
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            if [ -n "${{ github.event.inputs.version }}" ]; then
              echo "RELEASE_TAG=${{ github.event.inputs.version }}" >> $GITHUB_ENV
            else
              echo "No version provided for workflow_dispatch. Exiting." >&2
              exit 1
            fi
          else
            commit_msg=$(jq -r .head_commit.message < "$GITHUB_EVENT_PATH")
            echo "Commit message: $commit_msg"
            version=$(echo "$commit_msg" | grep -oE 'v[0-9]+\.[0-9]+\.[0-9]+' | head -n1 || true)
            if [ -n "$version" ]; then
              echo "RELEASE_TAG=$version" >> $GITHUB_ENV
            else
              echo "No version tag found in commit message. Exiting." >&2
              exit 1
            fi
          fi
      - name: Create GitHub Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          release_name: ${{ env.RELEASE_TAG }}
          body: "Release ${{ env.RELEASE_TAG }} generated by CI"
          draft: false
          prerelease: false
      - name: Upload APK to release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: release_artifacts/app-release.apk
          asset_name: emargator-${{ env.RELEASE_TAG }}.apk
          asset_content_type: application/vnd.android.package-archive
